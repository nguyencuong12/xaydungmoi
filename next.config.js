/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
  experimental: {
  },
  images:{
    domains:['mayxaydungmoi.com']
  }
}

module.exports = nextConfig
